
# QC Enterprise — Система контролю якості ПЗ

**QC Enterprise** — це веб-застосунок на базі Flask для автоматизованого аналізу якості коду, перевірки безпеки та управління проектами розробки. Система інтегрується з GitHub через вебхуки, генерує детальні звіти (PDF, графіки) та надсилає сповіщення у Telegram.

![Python](https://img.shields.io/badge/Python-3.10%2B-blue)
![Flask](https://img.shields.io/badge/Flask-2.0%2B-green)
![Coverage](https://img.shields.io/badge/Coverage-56%25-orange)

## 🚀 Основні можливості

* **🕵️ Автоматичний аналіз коду:**
    * **Pylint:** Перевірка якості коду та відповідності стандартам (PEP8).
    * **Radon:** Обчислення цикломатичної складності (Cyclomatic Complexity).
    * **Bandit:** Пошук поширених вразливостей безпеки.
* **📊 Візуалізація та звітність:**
    * Інтерактивні графіки динаміки якості проекту (Chart.js).
    * Генерація офіційних PDF-звітів.
    * Історія всіх перевірок.
* **🤝 Командна робота:**
    * Створення проектів та генерація інвайт-кодів.
    * Рольова модель (Адміністратор проекту / Учасник).
    * Гейміфікація (Ачівки: "Нічна сова", "Стахановець").
* **🔔 Інтеграції:**
    * **GitHub Webhooks:** Автоматичний запуск сканування при `git push`.
    * **Telegram Bot:** Сповіщення про результати перевірки та нагадування про Daily Meeting.
* **🛡️ Адмін-панель:** Зручний інтерфейс для управління користувачами та проектами.

## 🛠 Технічний стек

* **Backend:** Python, Flask, SQLAlchemy, Flask-Login, Flask-Admin.
* **Frontend:** HTML5, CSS3 (Bootstrap 5), Jinja2.
* **Database:** SQLite (SQLAlchemy ORM).
* **Testing:** Pytest, Playwright (E2E), Unittest.
* **Tools:** GitPython, APScheduler, Ngrok.

## ⚙️ Встановлення та запуск

### 1. Клонування репозиторію
```bash
git clone [https://github.com/your-username/quality-system.git](https://github.com/your-username/quality-system.git)
cd quality-system
````

### 2\. Створення віртуального середовища

```bash
# Windows
python -m venv venv
.\venv\Scripts\activate

# macOS/Linux
python3 -m venv venv
source venv/bin/activate
```

### 3\. Встановлення залежностей

```bash
pip install -r requirements.txt
```

### 4\. Налаштування змінних середовища (.env)

Створіть файл `.env` у корені проекту та додайте наступні налаштування:

```env
SECRET_KEY=your_secret_key_here
TG_TOKEN=your_telegram_bot_token
# TG_CHAT_ID= (опціонально, для адмінських сповіщень)
# OPENAI_API_KEY= (опціонально, для AI зведень)
```

### 5\. Ініціалізація бази даних

Створіть файл `site.db` та таблиці:

```bash
python init_db.py
```

*(Якщо файлу `init_db.py` немає, запустіть python консоль і виконайте: `from app import create_app, db; app=create_app(); app.app_context().push(); db.create_all()`)*

### 6\. Запуск сервера

```bash
python run.py
```

Додаток буде доступний за адресою: `http://127.0.0.1:5000`

-----

## 🧪 Тестування

Проект покритий різними видами тестів.

### Unit та Інтеграційні тести

Перевірка моделей, контролерів та логіки бази даних:

```bash
pytest tests.py
```

### End-to-End (E2E) тести

Повна імітація дій користувача у браузері (потребує встановленого Playwright):

```bash
# Встановити браузери (один раз)
playwright install

# Запуск тесту (з відображенням браузера)
pytest --headed test_e2e.py
```

### Перевірка покриття коду (Coverage)

```bash
pytest --cov=app --cov-report=html tests.py test_e2e.py
```

Звіт буде у папці `htmlcov/index.html`.

-----

## 🔗 Налаштування GitHub Webhook

Щоб система автоматично сканувала ваш код при пуші в репозиторій:

1.  Зареєструйтесь у системі QC Enterprise та перейдіть у **"Налаштування профілю"**.
2.  Скопіюйте **Payload URL** (якщо локально — використовуйте URL від ngrok, який виводиться в консолі при запуску) та **Secret**.
3.  Перейдіть у налаштування вашого репозиторію на GitHub -\> **Webhooks** -\> **Add webhook**.
4.  Вставте дані:
      * **Payload URL**: Ваше посилання (`.../webhook`).
      * **Content type**: `application/json`.
      * **Secret**: Ваш секретний ключ з профілю.
      * **Which events?**: Just the `push` event.
5.  Натисніть **Add webhook**.

Тепер при кожному `git push` система буде клонувати репозиторій, проводити аналіз та надсилати вам звіт\!
